'use client'

import styled from '@emotion/styled'
const Wrapper = styled.div`
  height: 50px;
  background-color: lightgreen;
`

export default function SignupButton(): JSX.Element {
  return <Wrapper>회원가입 버튼</Wrapper>
}
