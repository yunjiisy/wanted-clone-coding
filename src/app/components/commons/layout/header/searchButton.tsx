'use client'

import styled from '@emotion/styled'
const Wrapper = styled.div`
  height: 50px;
  background-color: lightgreen;
`

export default function SearchButton(): JSX.Element {
  return <Wrapper>검색 버튼</Wrapper>
}
