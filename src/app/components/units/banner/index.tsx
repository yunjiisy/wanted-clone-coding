'use client'

import styled from '@emotion/styled'
const Wrapper = styled.div`
  height: 100px;
  background-color: skyblue;
`

export default function Banner(): JSX.Element {
  return <Wrapper>여기는 베너</Wrapper>
}
